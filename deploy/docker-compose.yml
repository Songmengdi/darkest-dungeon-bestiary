services:
  bestiary:
    image: registry.cn-hangzhou.aliyuncs.com/songm_d/dd1-bestiary:latest
    container_name: dd1-bestiary
    restart: unless-stopped
    ports:
      - "8899:80"
    build:
      context: ..
      dockerfile: deploy/Dockerfile
